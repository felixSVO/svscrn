## Install

```bash
# install nodejs 14 LTS, Ubuntu
curl -sL https://deb.nodesource.com/setup_14.x | sudo -E bash -
sudo apt-get install -y nodejs

# install 
npm install git+https://github.com/felixSVO/svscrn.git

# run
svscrn

```
